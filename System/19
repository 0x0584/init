# first things is that systemctl list-unit-files list all services
# grep enabled and us the HOLY FUCKING AWK to print the results

eval systemctl list-unit-files --type=service | grep enabled | awk -F'.service' '{gsub("@","",$1); print $1}'